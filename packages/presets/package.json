{
  "name": "@mobrienv/autoloop-presets",
  "version": "0.7.0",
  "description": "Bundled preset definitions (data-only) for autoloop",
  "type": "module",
  "exports": {
    "./package.json": "./package.json"
  },
  "files": [
    "presets"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "echo 'autoloop-presets is data-only, nothing to build' >&2"
  },
  "publishConfig": {
    "access": "public"
  }
}
