# Plan: Add iteration-37 review note

1. Refresh `context.md`, `plan.md`, and `progress.md` for the iteration-37 memory-only objective.
2. Add exactly one learning entry with the requested iteration-37 review text.
3. Verify the new learning by matching the full text in `./.miniloops/memory.jsonl` via `./.miniloops/miniloops inspect memory --format md`.
4. Commit only `context.md`, `plan.md`, `progress.md`, and `./.miniloops/memory.jsonl`, then emit `task.complete`.
