{
  "name": "autoloop",
  "version": "0.0.1",
  "description": "Autonomous LLM loop harness — TypeScript port",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "autoloop": "bin/autoloop"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/.bin/vitest run",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/vitest"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5"
  }
}
