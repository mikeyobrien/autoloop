# Standalone routing repro fixture

This deterministic command-backend preset verifies that a compiled binary can
emit a handoff through its generated `$AUTOLOOP_BIN` wrapper and route the next
iteration to another role.
